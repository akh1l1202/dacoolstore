finalize=True at line 25 in functions.py: to let the GUI finalized, then you can run any code about GUI or after GUI initialized. Method Window. read will wait event from GUI, so it do nothing before any event, like click the button.

event, _ = window.read() at line 71: no values were taken as input from the user hence values is put as _.